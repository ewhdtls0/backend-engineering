spring:
  datasource:
    url: jdbc:h2:mem:seat-hold-test;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    hikari:
      maximum-pool-size: 6
  jpa:
    hibernate:
      ddl-auto: create-drop
logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn

